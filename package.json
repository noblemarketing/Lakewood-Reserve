{
  "name": "lakewood-reserve",
  "private": true,
  "description": "Lakewood Reserve static site with Netlify serverless rental checkout",
  "scripts": {
    "build": "npm install --omit=dev",
    "dev": "npx netlify dev"
  },
  "dependencies": {
    "stripe": "^17.7.0"
  }
}
