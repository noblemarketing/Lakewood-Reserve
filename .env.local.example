# Copy to `.env` for local `netlify dev`, or set in Netlify Site settings → Environment variables.

# Stripe — https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
# From `stripe listen` locally, or Stripe Dashboard → Webhooks → signing secret in production
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret

# Twilio — https://console.twilio.com/
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_FROM_NUMBER=+15551234567

# Shed lock code sent by SMS after verified payment (never expose to the browser)
SHED_CODE=0127

# Public site URL, no trailing slash (used for Stripe success/cancel redirects)
# Local: http://localhost:8888  |  Production: https://your-domain.com
SITE_URL=http://localhost:8888
